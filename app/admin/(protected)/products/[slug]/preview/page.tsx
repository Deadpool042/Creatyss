/**
 * Aperçu storefront admin — route volontaire, non publique.
 *
 * Cette page constitue un point d'entrée dédié pour consulter la fiche produit
 * telle qu'elle apparaîtrait en vitrine, depuis l'interface d'administration.
 * Elle n'est pas accessible aux visiteurs et n'est pas liée au tunnel d'achat.
 *
 * Statut : stub — à implémenter lors du lot "aperçu storefront admin".
 */
import React from "react";

function ProductPreview() {
  return <div>ProductPreview</div>;
}

export default ProductPreview;
